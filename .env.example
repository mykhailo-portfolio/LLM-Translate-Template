# LLM Translate template – environment variables
# Copy to .env and set values. Used by llm/translate.py.

# Provider: "openai" or "anthropic" or any other provider
LLM_TRANSLATE_PROVIDER=openai

# At least one provider key is required, depending on LLM_TRANSLATE_PROVIDER
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: model overrides (defaults shown but depends on the provider) 
# LLM_OPENAI_MODEL=gpt-4o-mini
# LLM_ANTHROPIC_MODEL=claude-sonnet-4
